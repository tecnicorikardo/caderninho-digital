rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regras simples - qualquer usuário autenticado pode fazer tudo
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}